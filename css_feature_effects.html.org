<div id="app">
    <section class="preview wrapper" :style="filterStyles">
        <h1>CSS Filter Effects</h1>
        <img src="https://picsum.photos/800/400/?random" alt="">
    </section>
    <section class="editor wrapper--mini">
        <form @submit.prevent>
            <fieldset v-for="filter in filters" :class="{active: filter.value != filter.default}">
                <label :for="filter.name">{{ filter.name | capitalize }}</label>
                <input type="range" :id="filter.name" :min="filter.min" :max="filter.max" v-model="filter.value">
                <span>{{ filter.value }}{{ filter.unit }}</span>
                <button v-if="filter.value != filter.default" class="reset" @click="reset(filter)"><i
                        class="fa fa-times"></i></button>
            </fieldset>
        </form>
    </section>
    <section class="code wrapper--mini" v-if="copyStyles">
        <h2>Grab the code</h2>
        <pre>
			<code>
.selector {
	{{ copyStyles }}
}
			</code>
		</pre>
    </section>
</div>